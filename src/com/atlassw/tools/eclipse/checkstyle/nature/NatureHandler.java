package com.atlassw.tools.eclipse.checkstyle.nature;

import org.eclipse.core.runtime.IProgressMonitor;

public class NatureHandler
{

    public void configureDeconfigureNatureJob_runInWorkspaceHandler(IProgressMonitor monitor){
        monitor.done();
    }
}
